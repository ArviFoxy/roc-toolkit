/*
  File autogenerated by gengetopt version 2.23
  generated with the following command:
  gengetopt --input=cmdline.ggo --file-name=cmdline --include-getopt --func-name=cmdline_parser --arg-struct-name=gengetopt_args_info 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#ifndef FIX_UNUSED
#define FIX_UNUSED(X) (void) (X) /* avoid warnings for unused params */
#endif


#include "cmdline.h"

const char *gengetopt_args_info_purpose = "";

const char *gengetopt_args_info_usage = "Usage: roc-send OPTIONS";

const char *gengetopt_args_info_versiontext = "";

const char *gengetopt_args_info_description = "";

const char *gengetopt_args_info_help[] = {
  "  -h, --help                    Print help and exit",
  "  -V, --version                 Print version and exit",
  "  -v, --verbose                 Increase verbosity level (may be used multiple\n                                  times)",
  "      --color=ENUM              Set colored logging mode for stderr output\n                                  (possible values=\"auto\", \"always\",\n                                  \"never\" default=`auto')",
  "  -L, --list-supported          List supported protocols, formats, etc.",
  "\nInput options:",
  "  -i, --input=IO_URI            Input file or device URI",
  "      --io-encoding=IO_ENCODING Input device encoding",
  "      --io-latency=TIME         Input device latency, TIME units",
  "      --io-frame-len=TIME       Input frame length, TIME units",
  "\nNetwork options:",
  "  -s, --source=NET_URI          Remote source endpoint to connect to",
  "  -r, --repair=NET_URI          Remote repair endpoint to connect to",
  "  -c, --control=NET_URI         Remote control endpoint to connect to",
  "      --miface=IPADDR           IPv4 or IPv6 address of the network interface\n                                  on which to join the multicast group",
  "      --reuseaddr               Enable SO_REUSEADDR when binding sockets",
  "\nEncoding options:",
  "      --packet-encoding=PKT_ENCODING\n                                Custom network packet encoding",
  "      --packet-len=TIME         Network packet length, TIME units",
  "      --fec-encoding=FEC_ENCODING\n                                FEC encoding, 'auto' to auto-detect from\n                                  network endpoints",
  "      --fec-block-src=INT       Number of source packets in FEC block",
  "      --fec-block-rpr=INT       Number of repair packets in FEC block",
  "      --latency-backend=ENUM    Which latency to use in latency tuner\n                                  (possible values=\"niq\", \"e2e\"\n                                  default=`niq')",
  "      --latency-profile=ENUM    Latency tuning profile  (possible\n                                  values=\"responsive\", \"gradual\",\n                                  \"intact\" default=`intact')",
  "      --resampler-backend=ENUM  Resampler backend  (possible values=\"auto\",\n                                  \"builtin\", \"speex\", \"speexdec\"\n                                  default=`auto')",
  "      --resampler-profile=ENUM  Resampler profile  (possible values=\"low\",\n                                  \"medium\", \"high\" default=`medium')",
  "\nLatency options (for sender-side latency tuning):",
  "      --target-latency=TIME     Target latency, TIME units or 'auto' for\n                                  adaptive mode",
  "      --latency-tolerance=TIME  Maximum deviation from target latency, TIME\n                                  units",
  "      --start-latency=TIME      Starting target latency in adaptive mode, TIME\n                                  units",
  "      --min-latency=TIME        Minimum target latency in adaptive mode, TIME\n                                  units",
  "      --max-latency=TIME        Maximum target latency in adaptive mode, TIME\n                                  units",
  "\nTimestamp options:",
  "      --auto-cts                Automatically stamp frames with capture\n                                  timestamps (required for E2E latency)\n                                  (default=off)",
  "\nMetrics options:",
  "      --prometheus-metrics-port=INT\n                                Port for prometheus metrics HTTP exposer\n                                  (default=`0')",
  "      --prometheus-niq-latency-buckets=INT\n                                Number of histogram buckets for NIQ latency\n                                  metrics  (default=`100')",
  "      --prometheus-niq-latency-min=TIME\n                                Minimum NIQ latency bucket boundary, TIME units\n                                  (default=`5ms')",
  "      --prometheus-niq-latency-max=TIME\n                                Maximum NIQ latency bucket boundary, TIME units\n                                  (default=`50ms')",
  "      --prometheus-niq-latency-scale=ENUM\n                                Bucket spacing for NIQ latency histogram\n                                  (possible values=\"log\", \"linear\"\n                                  default=`log')",
  "      --prometheus-e2e-latency-buckets=INT\n                                Number of histogram buckets for E2E latency\n                                  metrics  (default=`100')",
  "      --prometheus-e2e-latency-min=TIME\n                                Minimum E2E latency bucket boundary, TIME units\n                                  (default=`20ms')",
  "      --prometheus-e2e-latency-max=TIME\n                                Maximum E2E latency bucket boundary, TIME units\n                                  (default=`200ms')",
  "      --prometheus-e2e-latency-scale=ENUM\n                                Bucket spacing for E2E latency histogram\n                                  (possible values=\"log\", \"linear\"\n                                  default=`log')",
  "      --prometheus-jitter-buckets=INT\n                                Number of histogram buckets for jitter metrics\n                                  (default=`100')",
  "      --prometheus-jitter-min=TIME\n                                Minimum jitter bucket boundary, TIME units\n                                  (default=`100us')",
  "      --prometheus-jitter-max=TIME\n                                Maximum jitter bucket boundary, TIME units\n                                  (default=`200ms')",
  "      --prometheus-jitter-scale=ENUM\n                                Bucket spacing for jitter histogram  (possible\n                                  values=\"log\", \"linear\" default=`log')",
  "      --prometheus-rtt-buckets=INT\n                                Number of histogram buckets for RTT metrics\n                                  (default=`100')",
  "      --prometheus-rtt-min=TIME Minimum RTT bucket boundary, TIME units\n                                  (default=`1ms')",
  "      --prometheus-rtt-max=TIME Maximum RTT bucket boundary, TIME units\n                                  (default=`100ms')",
  "      --prometheus-rtt-scale=ENUM\n                                Bucket spacing for RTT histogram  (possible\n                                  values=\"log\", \"linear\" default=`log')",
  "\nMemory options:",
  "      --max-packet-size=SIZE    Maximum network packet size, SIZE units",
  "      --max-frame-size=SIZE     Maximum I/O and processing frame size, SIZE\n                                  units",
  "\nDebugging options:",
  "      --prof                    Enable self-profiling  (default=off)",
  "      --dump=PATH               Dump run-time metrics to specified CSV file",
  "\nIO_URI is input device or file URI in forms:\n    <driver>://<device>; file://<path>; file:<path>\n  Examples:\n    alsa://default; pulse://alsa_input.pci-0000_00_1f.3.analog-stereo\n    file:///home/user/test.wav; file:./test.wav; file:-\n  (use '<driver>://default' for default device, 'file://-' for stdin)\n\nIO_ENCODING is input device or file encoding in form:\n     <format>[@<subformat>]/<rate>/<channels>\n  Examples:\n    pcm@s16/44100/mono; wav/-/-\n  (any component may be '-' to use default value)\n\nNET_URI is network endpoint URI in form:\n    <protocol>://<host>[:<port>][/<path>][?<query>]\n  Examples:\n    rtp://0.0.0.0:10001; rtp+rs8m://127.0.0.1:10001; rs8m://[::1]:10001\n\nPKT_ENCODING is media packets encoding in form:\n    <id>:<format>[@<subformat>]/<rate>/<channels>:\n  Examples:\n    101:pcm@s16/44100/mono; 102:flac@s24/48000/stereo\n  (numeric id is arbitrary number in range 100..127 that should be same\n  on both sender and receiver to identify encoding)\n\nFEC_ENCODING is repair packets encoding name:\n  auto; rs8m; ldpc\n  (use 'auto' to auto-detect fec encoding from network URIs)\n\nTIME defines duration using a number with mandatory suffix:\n  123ns; 1.23us; 1.23ms; 1.23s; 1.23m; 1.23h;\n\nSIZE defines byte size using a number with optional suffix:\n  123; 1.23K; 1.23M; 1.23G;\n\nUse --list-supported (-L) option to print the list of the supported\nprotocols, formats, channel masks, etc.\n\nSee further details in roc-send(1) manual page locally or online:\nhttps://roc-streaming.org/toolkit/docs/manuals/roc_send.html",
    0
};

typedef enum {ARG_NO
  , ARG_FLAG
  , ARG_STRING
  , ARG_INT
  , ARG_ENUM
} cmdline_parser_arg_type;

static
void clear_given (struct gengetopt_args_info *args_info);
static
void clear_args (struct gengetopt_args_info *args_info);

static int
cmdline_parser_internal (int argc, char **argv, struct gengetopt_args_info *args_info,
                        struct cmdline_parser_params *params, const char *additional_error);

static int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error);

const char *cmdline_parser_color_values[] = {"auto", "always", "never", 0}; /*< Possible values for color. */
const char *cmdline_parser_latency_backend_values[] = {"niq", "e2e", 0}; /*< Possible values for latency-backend. */
const char *cmdline_parser_latency_profile_values[] = {"responsive", "gradual", "intact", 0}; /*< Possible values for latency-profile. */
const char *cmdline_parser_resampler_backend_values[] = {"auto", "builtin", "speex", "speexdec", 0}; /*< Possible values for resampler-backend. */
const char *cmdline_parser_resampler_profile_values[] = {"low", "medium", "high", 0}; /*< Possible values for resampler-profile. */
const char *cmdline_parser_prometheus_niq_latency_scale_values[] = {"log", "linear", 0}; /*< Possible values for prometheus-niq-latency-scale. */
const char *cmdline_parser_prometheus_e2e_latency_scale_values[] = {"log", "linear", 0}; /*< Possible values for prometheus-e2e-latency-scale. */
const char *cmdline_parser_prometheus_jitter_scale_values[] = {"log", "linear", 0}; /*< Possible values for prometheus-jitter-scale. */
const char *cmdline_parser_prometheus_rtt_scale_values[] = {"log", "linear", 0}; /*< Possible values for prometheus-rtt-scale. */

static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct gengetopt_args_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->verbose_given = 0 ;
  args_info->color_given = 0 ;
  args_info->list_supported_given = 0 ;
  args_info->input_given = 0 ;
  args_info->io_encoding_given = 0 ;
  args_info->io_latency_given = 0 ;
  args_info->io_frame_len_given = 0 ;
  args_info->source_given = 0 ;
  args_info->repair_given = 0 ;
  args_info->control_given = 0 ;
  args_info->miface_given = 0 ;
  args_info->reuseaddr_given = 0 ;
  args_info->packet_encoding_given = 0 ;
  args_info->packet_len_given = 0 ;
  args_info->fec_encoding_given = 0 ;
  args_info->fec_block_src_given = 0 ;
  args_info->fec_block_rpr_given = 0 ;
  args_info->latency_backend_given = 0 ;
  args_info->latency_profile_given = 0 ;
  args_info->resampler_backend_given = 0 ;
  args_info->resampler_profile_given = 0 ;
  args_info->target_latency_given = 0 ;
  args_info->latency_tolerance_given = 0 ;
  args_info->start_latency_given = 0 ;
  args_info->min_latency_given = 0 ;
  args_info->max_latency_given = 0 ;
  args_info->auto_cts_given = 0 ;
  args_info->prometheus_metrics_port_given = 0 ;
  args_info->prometheus_niq_latency_buckets_given = 0 ;
  args_info->prometheus_niq_latency_min_given = 0 ;
  args_info->prometheus_niq_latency_max_given = 0 ;
  args_info->prometheus_niq_latency_scale_given = 0 ;
  args_info->prometheus_e2e_latency_buckets_given = 0 ;
  args_info->prometheus_e2e_latency_min_given = 0 ;
  args_info->prometheus_e2e_latency_max_given = 0 ;
  args_info->prometheus_e2e_latency_scale_given = 0 ;
  args_info->prometheus_jitter_buckets_given = 0 ;
  args_info->prometheus_jitter_min_given = 0 ;
  args_info->prometheus_jitter_max_given = 0 ;
  args_info->prometheus_jitter_scale_given = 0 ;
  args_info->prometheus_rtt_buckets_given = 0 ;
  args_info->prometheus_rtt_min_given = 0 ;
  args_info->prometheus_rtt_max_given = 0 ;
  args_info->prometheus_rtt_scale_given = 0 ;
  args_info->max_packet_size_given = 0 ;
  args_info->max_frame_size_given = 0 ;
  args_info->prof_given = 0 ;
  args_info->dump_given = 0 ;
}

static
void clear_args (struct gengetopt_args_info *args_info)
{
  FIX_UNUSED (args_info);
  args_info->color_arg = color_arg_auto;
  args_info->color_orig = NULL;
  args_info->input_arg = NULL;
  args_info->input_orig = NULL;
  args_info->io_encoding_arg = NULL;
  args_info->io_encoding_orig = NULL;
  args_info->io_latency_arg = NULL;
  args_info->io_latency_orig = NULL;
  args_info->io_frame_len_arg = NULL;
  args_info->io_frame_len_orig = NULL;
  args_info->source_arg = NULL;
  args_info->source_orig = NULL;
  args_info->repair_arg = NULL;
  args_info->repair_orig = NULL;
  args_info->control_arg = NULL;
  args_info->control_orig = NULL;
  args_info->miface_arg = NULL;
  args_info->miface_orig = NULL;
  args_info->packet_encoding_arg = NULL;
  args_info->packet_encoding_orig = NULL;
  args_info->packet_len_arg = NULL;
  args_info->packet_len_orig = NULL;
  args_info->fec_encoding_arg = NULL;
  args_info->fec_encoding_orig = NULL;
  args_info->fec_block_src_orig = NULL;
  args_info->fec_block_rpr_orig = NULL;
  args_info->latency_backend_arg = latency_backend_arg_niq;
  args_info->latency_backend_orig = NULL;
  args_info->latency_profile_arg = latency_profile_arg_intact;
  args_info->latency_profile_orig = NULL;
  args_info->resampler_backend_arg = resampler_backend_arg_auto;
  args_info->resampler_backend_orig = NULL;
  args_info->resampler_profile_arg = resampler_profile_arg_medium;
  args_info->resampler_profile_orig = NULL;
  args_info->target_latency_arg = NULL;
  args_info->target_latency_orig = NULL;
  args_info->latency_tolerance_arg = NULL;
  args_info->latency_tolerance_orig = NULL;
  args_info->start_latency_arg = NULL;
  args_info->start_latency_orig = NULL;
  args_info->min_latency_arg = NULL;
  args_info->min_latency_orig = NULL;
  args_info->max_latency_arg = NULL;
  args_info->max_latency_orig = NULL;
  args_info->auto_cts_flag = 0;
  args_info->prometheus_metrics_port_arg = 0;
  args_info->prometheus_metrics_port_orig = NULL;
  args_info->prometheus_niq_latency_buckets_arg = 100;
  args_info->prometheus_niq_latency_buckets_orig = NULL;
  args_info->prometheus_niq_latency_min_arg = gengetopt_strdup ("5ms");
  args_info->prometheus_niq_latency_min_orig = NULL;
  args_info->prometheus_niq_latency_max_arg = gengetopt_strdup ("50ms");
  args_info->prometheus_niq_latency_max_orig = NULL;
  args_info->prometheus_niq_latency_scale_arg = prometheus_niq_latency_scale_arg_log;
  args_info->prometheus_niq_latency_scale_orig = NULL;
  args_info->prometheus_e2e_latency_buckets_arg = 100;
  args_info->prometheus_e2e_latency_buckets_orig = NULL;
  args_info->prometheus_e2e_latency_min_arg = gengetopt_strdup ("20ms");
  args_info->prometheus_e2e_latency_min_orig = NULL;
  args_info->prometheus_e2e_latency_max_arg = gengetopt_strdup ("200ms");
  args_info->prometheus_e2e_latency_max_orig = NULL;
  args_info->prometheus_e2e_latency_scale_arg = prometheus_e2e_latency_scale_arg_log;
  args_info->prometheus_e2e_latency_scale_orig = NULL;
  args_info->prometheus_jitter_buckets_arg = 100;
  args_info->prometheus_jitter_buckets_orig = NULL;
  args_info->prometheus_jitter_min_arg = gengetopt_strdup ("100us");
  args_info->prometheus_jitter_min_orig = NULL;
  args_info->prometheus_jitter_max_arg = gengetopt_strdup ("200ms");
  args_info->prometheus_jitter_max_orig = NULL;
  args_info->prometheus_jitter_scale_arg = prometheus_jitter_scale_arg_log;
  args_info->prometheus_jitter_scale_orig = NULL;
  args_info->prometheus_rtt_buckets_arg = 100;
  args_info->prometheus_rtt_buckets_orig = NULL;
  args_info->prometheus_rtt_min_arg = gengetopt_strdup ("1ms");
  args_info->prometheus_rtt_min_orig = NULL;
  args_info->prometheus_rtt_max_arg = gengetopt_strdup ("100ms");
  args_info->prometheus_rtt_max_orig = NULL;
  args_info->prometheus_rtt_scale_arg = prometheus_rtt_scale_arg_log;
  args_info->prometheus_rtt_scale_orig = NULL;
  args_info->max_packet_size_arg = NULL;
  args_info->max_packet_size_orig = NULL;
  args_info->max_frame_size_arg = NULL;
  args_info->max_frame_size_orig = NULL;
  args_info->prof_flag = 0;
  args_info->dump_arg = NULL;
  args_info->dump_orig = NULL;
  
}

static
void init_args_info(struct gengetopt_args_info *args_info)
{


  args_info->help_help = gengetopt_args_info_help[0] ;
  args_info->version_help = gengetopt_args_info_help[1] ;
  args_info->verbose_help = gengetopt_args_info_help[2] ;
  args_info->verbose_min = 0;
  args_info->verbose_max = 0;
  args_info->color_help = gengetopt_args_info_help[3] ;
  args_info->list_supported_help = gengetopt_args_info_help[4] ;
  args_info->input_help = gengetopt_args_info_help[6] ;
  args_info->io_encoding_help = gengetopt_args_info_help[7] ;
  args_info->io_latency_help = gengetopt_args_info_help[8] ;
  args_info->io_frame_len_help = gengetopt_args_info_help[9] ;
  args_info->source_help = gengetopt_args_info_help[11] ;
  args_info->source_min = 0;
  args_info->source_max = 0;
  args_info->repair_help = gengetopt_args_info_help[12] ;
  args_info->repair_min = 0;
  args_info->repair_max = 0;
  args_info->control_help = gengetopt_args_info_help[13] ;
  args_info->control_min = 0;
  args_info->control_max = 0;
  args_info->miface_help = gengetopt_args_info_help[14] ;
  args_info->miface_min = 0;
  args_info->miface_max = 0;
  args_info->reuseaddr_help = gengetopt_args_info_help[15] ;
  args_info->packet_encoding_help = gengetopt_args_info_help[17] ;
  args_info->packet_len_help = gengetopt_args_info_help[18] ;
  args_info->fec_encoding_help = gengetopt_args_info_help[19] ;
  args_info->fec_block_src_help = gengetopt_args_info_help[20] ;
  args_info->fec_block_rpr_help = gengetopt_args_info_help[21] ;
  args_info->latency_backend_help = gengetopt_args_info_help[22] ;
  args_info->latency_profile_help = gengetopt_args_info_help[23] ;
  args_info->resampler_backend_help = gengetopt_args_info_help[24] ;
  args_info->resampler_profile_help = gengetopt_args_info_help[25] ;
  args_info->target_latency_help = gengetopt_args_info_help[27] ;
  args_info->latency_tolerance_help = gengetopt_args_info_help[28] ;
  args_info->start_latency_help = gengetopt_args_info_help[29] ;
  args_info->min_latency_help = gengetopt_args_info_help[30] ;
  args_info->max_latency_help = gengetopt_args_info_help[31] ;
  args_info->auto_cts_help = gengetopt_args_info_help[33] ;
  args_info->prometheus_metrics_port_help = gengetopt_args_info_help[35] ;
  args_info->prometheus_niq_latency_buckets_help = gengetopt_args_info_help[36] ;
  args_info->prometheus_niq_latency_min_help = gengetopt_args_info_help[37] ;
  args_info->prometheus_niq_latency_max_help = gengetopt_args_info_help[38] ;
  args_info->prometheus_niq_latency_scale_help = gengetopt_args_info_help[39] ;
  args_info->prometheus_e2e_latency_buckets_help = gengetopt_args_info_help[40] ;
  args_info->prometheus_e2e_latency_min_help = gengetopt_args_info_help[41] ;
  args_info->prometheus_e2e_latency_max_help = gengetopt_args_info_help[42] ;
  args_info->prometheus_e2e_latency_scale_help = gengetopt_args_info_help[43] ;
  args_info->prometheus_jitter_buckets_help = gengetopt_args_info_help[44] ;
  args_info->prometheus_jitter_min_help = gengetopt_args_info_help[45] ;
  args_info->prometheus_jitter_max_help = gengetopt_args_info_help[46] ;
  args_info->prometheus_jitter_scale_help = gengetopt_args_info_help[47] ;
  args_info->prometheus_rtt_buckets_help = gengetopt_args_info_help[48] ;
  args_info->prometheus_rtt_min_help = gengetopt_args_info_help[49] ;
  args_info->prometheus_rtt_max_help = gengetopt_args_info_help[50] ;
  args_info->prometheus_rtt_scale_help = gengetopt_args_info_help[51] ;
  args_info->max_packet_size_help = gengetopt_args_info_help[53] ;
  args_info->max_frame_size_help = gengetopt_args_info_help[54] ;
  args_info->prof_help = gengetopt_args_info_help[56] ;
  args_info->dump_help = gengetopt_args_info_help[57] ;
  
}

void
cmdline_parser_print_version (void)
{
  printf ("%s %s\n",
     (strlen(CMDLINE_PARSER_PACKAGE_NAME) ? CMDLINE_PARSER_PACKAGE_NAME : CMDLINE_PARSER_PACKAGE),
     CMDLINE_PARSER_VERSION);

  if (strlen(gengetopt_args_info_versiontext) > 0)
    printf("\n%s\n", gengetopt_args_info_versiontext);
}

static void print_help_common(void)
{
	size_t len_purpose = strlen(gengetopt_args_info_purpose);
	size_t len_usage = strlen(gengetopt_args_info_usage);

	if (len_usage > 0) {
		printf("%s\n", gengetopt_args_info_usage);
	}
	if (len_purpose > 0) {
		printf("%s\n", gengetopt_args_info_purpose);
	}

	if (len_usage || len_purpose) {
		printf("\n");
	}

	if (strlen(gengetopt_args_info_description) > 0) {
		printf("%s\n\n", gengetopt_args_info_description);
	}
}

void
cmdline_parser_print_help (void)
{
  int i = 0;
  print_help_common();
  while (gengetopt_args_info_help[i])
    printf("%s\n", gengetopt_args_info_help[i++]);
}

void
cmdline_parser_init (struct gengetopt_args_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);
  init_args_info (args_info);
}

void
cmdline_parser_params_init(struct cmdline_parser_params *params)
{
  if (params)
    { 
      params->override = 0;
      params->initialize = 1;
      params->check_required = 1;
      params->check_ambiguity = 0;
      params->print_errors = 1;
    }
}

struct cmdline_parser_params *
cmdline_parser_params_create(void)
{
  struct cmdline_parser_params *params = 
    (struct cmdline_parser_params *)malloc(sizeof(struct cmdline_parser_params));
  cmdline_parser_params_init(params);  
  return params;
}

static void
free_string_field (char **s)
{
  if (*s)
    {
      free (*s);
      *s = 0;
    }
}

/** @brief generic value variable */
union generic_value {
    int int_arg;
    char *string_arg;
    const char *default_string_arg;
};

/** @brief holds temporary values for multiple options */
struct generic_list
{
  union generic_value arg;
  char *orig;
  struct generic_list *next;
};

/**
 * @brief add a node at the head of the list 
 */
static void add_node(struct generic_list **list) {
  struct generic_list *new_node = (struct generic_list *) malloc (sizeof (struct generic_list));
  new_node->next = *list;
  *list = new_node;
  new_node->arg.string_arg = 0;
  new_node->orig = 0;
}


static void
free_multiple_string_field(unsigned int len, char ***arg, char ***orig)
{
  unsigned int i;
  if (*arg) {
    for (i = 0; i < len; ++i)
      {
        free_string_field(&((*arg)[i]));
        free_string_field(&((*orig)[i]));
      }
    free_string_field(&((*arg)[0])); /* free default string */

    free (*arg);
    *arg = 0;
    free (*orig);
    *orig = 0;
  }
}

static void
cmdline_parser_release (struct gengetopt_args_info *args_info)
{

  free_string_field (&(args_info->color_orig));
  free_string_field (&(args_info->input_arg));
  free_string_field (&(args_info->input_orig));
  free_string_field (&(args_info->io_encoding_arg));
  free_string_field (&(args_info->io_encoding_orig));
  free_string_field (&(args_info->io_latency_arg));
  free_string_field (&(args_info->io_latency_orig));
  free_string_field (&(args_info->io_frame_len_arg));
  free_string_field (&(args_info->io_frame_len_orig));
  free_multiple_string_field (args_info->source_given, &(args_info->source_arg), &(args_info->source_orig));
  free_multiple_string_field (args_info->repair_given, &(args_info->repair_arg), &(args_info->repair_orig));
  free_multiple_string_field (args_info->control_given, &(args_info->control_arg), &(args_info->control_orig));
  free_multiple_string_field (args_info->miface_given, &(args_info->miface_arg), &(args_info->miface_orig));
  free_string_field (&(args_info->packet_encoding_arg));
  free_string_field (&(args_info->packet_encoding_orig));
  free_string_field (&(args_info->packet_len_arg));
  free_string_field (&(args_info->packet_len_orig));
  free_string_field (&(args_info->fec_encoding_arg));
  free_string_field (&(args_info->fec_encoding_orig));
  free_string_field (&(args_info->fec_block_src_orig));
  free_string_field (&(args_info->fec_block_rpr_orig));
  free_string_field (&(args_info->latency_backend_orig));
  free_string_field (&(args_info->latency_profile_orig));
  free_string_field (&(args_info->resampler_backend_orig));
  free_string_field (&(args_info->resampler_profile_orig));
  free_string_field (&(args_info->target_latency_arg));
  free_string_field (&(args_info->target_latency_orig));
  free_string_field (&(args_info->latency_tolerance_arg));
  free_string_field (&(args_info->latency_tolerance_orig));
  free_string_field (&(args_info->start_latency_arg));
  free_string_field (&(args_info->start_latency_orig));
  free_string_field (&(args_info->min_latency_arg));
  free_string_field (&(args_info->min_latency_orig));
  free_string_field (&(args_info->max_latency_arg));
  free_string_field (&(args_info->max_latency_orig));
  free_string_field (&(args_info->prometheus_metrics_port_orig));
  free_string_field (&(args_info->prometheus_niq_latency_buckets_orig));
  free_string_field (&(args_info->prometheus_niq_latency_min_arg));
  free_string_field (&(args_info->prometheus_niq_latency_min_orig));
  free_string_field (&(args_info->prometheus_niq_latency_max_arg));
  free_string_field (&(args_info->prometheus_niq_latency_max_orig));
  free_string_field (&(args_info->prometheus_niq_latency_scale_orig));
  free_string_field (&(args_info->prometheus_e2e_latency_buckets_orig));
  free_string_field (&(args_info->prometheus_e2e_latency_min_arg));
  free_string_field (&(args_info->prometheus_e2e_latency_min_orig));
  free_string_field (&(args_info->prometheus_e2e_latency_max_arg));
  free_string_field (&(args_info->prometheus_e2e_latency_max_orig));
  free_string_field (&(args_info->prometheus_e2e_latency_scale_orig));
  free_string_field (&(args_info->prometheus_jitter_buckets_orig));
  free_string_field (&(args_info->prometheus_jitter_min_arg));
  free_string_field (&(args_info->prometheus_jitter_min_orig));
  free_string_field (&(args_info->prometheus_jitter_max_arg));
  free_string_field (&(args_info->prometheus_jitter_max_orig));
  free_string_field (&(args_info->prometheus_jitter_scale_orig));
  free_string_field (&(args_info->prometheus_rtt_buckets_orig));
  free_string_field (&(args_info->prometheus_rtt_min_arg));
  free_string_field (&(args_info->prometheus_rtt_min_orig));
  free_string_field (&(args_info->prometheus_rtt_max_arg));
  free_string_field (&(args_info->prometheus_rtt_max_orig));
  free_string_field (&(args_info->prometheus_rtt_scale_orig));
  free_string_field (&(args_info->max_packet_size_arg));
  free_string_field (&(args_info->max_packet_size_orig));
  free_string_field (&(args_info->max_frame_size_arg));
  free_string_field (&(args_info->max_frame_size_orig));
  free_string_field (&(args_info->dump_arg));
  free_string_field (&(args_info->dump_orig));
  
  

  clear_given (args_info);
}

/**
 * @param val the value to check
 * @param values the possible values
 * @return the index of the matched value:
 * -1 if no value matched,
 * -2 if more than one value has matched
 */
static int
check_possible_values(const char *val, const char *values[])
{
  int i, found, last;
  size_t len;

  if (!val)   /* otherwise strlen() crashes below */
    return -1; /* -1 means no argument for the option */

  found = last = 0;

  for (i = 0, len = strlen(val); values[i]; ++i)
    {
      if (strncmp(val, values[i], len) == 0)
        {
          ++found;
          last = i;
          if (strlen(values[i]) == len)
            return i; /* exact macth no need to check more */
        }
    }

  if (found == 1) /* one match: OK */
    return last;

  return (found ? -2 : -1); /* return many values or none matched */
}


static void
write_into_file(FILE *outfile, const char *opt, const char *arg, const char *values[])
{
  int found = -1;
  if (arg) {
    if (values) {
      found = check_possible_values(arg, values);      
    }
    if (found >= 0)
      fprintf(outfile, "%s=\"%s\" # %s\n", opt, arg, values[found]);
    else
      fprintf(outfile, "%s=\"%s\"\n", opt, arg);
  } else {
    fprintf(outfile, "%s\n", opt);
  }
}

static void
write_multiple_into_file(FILE *outfile, int len, const char *opt, char **arg, const char *values[])
{
  int i;
  
  for (i = 0; i < len; ++i)
    write_into_file(outfile, opt, (arg ? arg[i] : 0), values);
}

int
cmdline_parser_dump(FILE *outfile, struct gengetopt_args_info *args_info)
{
  int i = 0;

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot dump options to stream\n", CMDLINE_PARSER_PACKAGE);
      return EXIT_FAILURE;
    }

  if (args_info->help_given)
    write_into_file(outfile, "help", 0, 0 );
  if (args_info->version_given)
    write_into_file(outfile, "version", 0, 0 );
  write_multiple_into_file(outfile, args_info->verbose_given, "verbose", 0, 0);
  if (args_info->color_given)
    write_into_file(outfile, "color", args_info->color_orig, cmdline_parser_color_values);
  if (args_info->list_supported_given)
    write_into_file(outfile, "list-supported", 0, 0 );
  if (args_info->input_given)
    write_into_file(outfile, "input", args_info->input_orig, 0);
  if (args_info->io_encoding_given)
    write_into_file(outfile, "io-encoding", args_info->io_encoding_orig, 0);
  if (args_info->io_latency_given)
    write_into_file(outfile, "io-latency", args_info->io_latency_orig, 0);
  if (args_info->io_frame_len_given)
    write_into_file(outfile, "io-frame-len", args_info->io_frame_len_orig, 0);
  write_multiple_into_file(outfile, args_info->source_given, "source", args_info->source_orig, 0);
  write_multiple_into_file(outfile, args_info->repair_given, "repair", args_info->repair_orig, 0);
  write_multiple_into_file(outfile, args_info->control_given, "control", args_info->control_orig, 0);
  write_multiple_into_file(outfile, args_info->miface_given, "miface", args_info->miface_orig, 0);
  if (args_info->reuseaddr_given)
    write_into_file(outfile, "reuseaddr", 0, 0 );
  if (args_info->packet_encoding_given)
    write_into_file(outfile, "packet-encoding", args_info->packet_encoding_orig, 0);
  if (args_info->packet_len_given)
    write_into_file(outfile, "packet-len", args_info->packet_len_orig, 0);
  if (args_info->fec_encoding_given)
    write_into_file(outfile, "fec-encoding", args_info->fec_encoding_orig, 0);
  if (args_info->fec_block_src_given)
    write_into_file(outfile, "fec-block-src", args_info->fec_block_src_orig, 0);
  if (args_info->fec_block_rpr_given)
    write_into_file(outfile, "fec-block-rpr", args_info->fec_block_rpr_orig, 0);
  if (args_info->latency_backend_given)
    write_into_file(outfile, "latency-backend", args_info->latency_backend_orig, cmdline_parser_latency_backend_values);
  if (args_info->latency_profile_given)
    write_into_file(outfile, "latency-profile", args_info->latency_profile_orig, cmdline_parser_latency_profile_values);
  if (args_info->resampler_backend_given)
    write_into_file(outfile, "resampler-backend", args_info->resampler_backend_orig, cmdline_parser_resampler_backend_values);
  if (args_info->resampler_profile_given)
    write_into_file(outfile, "resampler-profile", args_info->resampler_profile_orig, cmdline_parser_resampler_profile_values);
  if (args_info->target_latency_given)
    write_into_file(outfile, "target-latency", args_info->target_latency_orig, 0);
  if (args_info->latency_tolerance_given)
    write_into_file(outfile, "latency-tolerance", args_info->latency_tolerance_orig, 0);
  if (args_info->start_latency_given)
    write_into_file(outfile, "start-latency", args_info->start_latency_orig, 0);
  if (args_info->min_latency_given)
    write_into_file(outfile, "min-latency", args_info->min_latency_orig, 0);
  if (args_info->max_latency_given)
    write_into_file(outfile, "max-latency", args_info->max_latency_orig, 0);
  if (args_info->auto_cts_given)
    write_into_file(outfile, "auto-cts", 0, 0 );
  if (args_info->prometheus_metrics_port_given)
    write_into_file(outfile, "prometheus-metrics-port", args_info->prometheus_metrics_port_orig, 0);
  if (args_info->prometheus_niq_latency_buckets_given)
    write_into_file(outfile, "prometheus-niq-latency-buckets", args_info->prometheus_niq_latency_buckets_orig, 0);
  if (args_info->prometheus_niq_latency_min_given)
    write_into_file(outfile, "prometheus-niq-latency-min", args_info->prometheus_niq_latency_min_orig, 0);
  if (args_info->prometheus_niq_latency_max_given)
    write_into_file(outfile, "prometheus-niq-latency-max", args_info->prometheus_niq_latency_max_orig, 0);
  if (args_info->prometheus_niq_latency_scale_given)
    write_into_file(outfile, "prometheus-niq-latency-scale", args_info->prometheus_niq_latency_scale_orig, cmdline_parser_prometheus_niq_latency_scale_values);
  if (args_info->prometheus_e2e_latency_buckets_given)
    write_into_file(outfile, "prometheus-e2e-latency-buckets", args_info->prometheus_e2e_latency_buckets_orig, 0);
  if (args_info->prometheus_e2e_latency_min_given)
    write_into_file(outfile, "prometheus-e2e-latency-min", args_info->prometheus_e2e_latency_min_orig, 0);
  if (args_info->prometheus_e2e_latency_max_given)
    write_into_file(outfile, "prometheus-e2e-latency-max", args_info->prometheus_e2e_latency_max_orig, 0);
  if (args_info->prometheus_e2e_latency_scale_given)
    write_into_file(outfile, "prometheus-e2e-latency-scale", args_info->prometheus_e2e_latency_scale_orig, cmdline_parser_prometheus_e2e_latency_scale_values);
  if (args_info->prometheus_jitter_buckets_given)
    write_into_file(outfile, "prometheus-jitter-buckets", args_info->prometheus_jitter_buckets_orig, 0);
  if (args_info->prometheus_jitter_min_given)
    write_into_file(outfile, "prometheus-jitter-min", args_info->prometheus_jitter_min_orig, 0);
  if (args_info->prometheus_jitter_max_given)
    write_into_file(outfile, "prometheus-jitter-max", args_info->prometheus_jitter_max_orig, 0);
  if (args_info->prometheus_jitter_scale_given)
    write_into_file(outfile, "prometheus-jitter-scale", args_info->prometheus_jitter_scale_orig, cmdline_parser_prometheus_jitter_scale_values);
  if (args_info->prometheus_rtt_buckets_given)
    write_into_file(outfile, "prometheus-rtt-buckets", args_info->prometheus_rtt_buckets_orig, 0);
  if (args_info->prometheus_rtt_min_given)
    write_into_file(outfile, "prometheus-rtt-min", args_info->prometheus_rtt_min_orig, 0);
  if (args_info->prometheus_rtt_max_given)
    write_into_file(outfile, "prometheus-rtt-max", args_info->prometheus_rtt_max_orig, 0);
  if (args_info->prometheus_rtt_scale_given)
    write_into_file(outfile, "prometheus-rtt-scale", args_info->prometheus_rtt_scale_orig, cmdline_parser_prometheus_rtt_scale_values);
  if (args_info->max_packet_size_given)
    write_into_file(outfile, "max-packet-size", args_info->max_packet_size_orig, 0);
  if (args_info->max_frame_size_given)
    write_into_file(outfile, "max-frame-size", args_info->max_frame_size_orig, 0);
  if (args_info->prof_given)
    write_into_file(outfile, "prof", 0, 0 );
  if (args_info->dump_given)
    write_into_file(outfile, "dump", args_info->dump_orig, 0);
  

  i = EXIT_SUCCESS;
  return i;
}

int
cmdline_parser_file_save(const char *filename, struct gengetopt_args_info *args_info)
{
  FILE *outfile;
  int i = 0;

  outfile = fopen(filename, "w");

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot open file for writing: %s\n", CMDLINE_PARSER_PACKAGE, filename);
      return EXIT_FAILURE;
    }

  i = cmdline_parser_dump(outfile, args_info);
  fclose (outfile);

  return i;
}

void
cmdline_parser_free (struct gengetopt_args_info *args_info)
{
  cmdline_parser_release (args_info);
}

/** @brief replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = 0;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

static char *
get_multiple_arg_token(const char *arg)
{
  const char *tok;
  char *ret;
  size_t len, num_of_escape, i, j;

  if (!arg)
    return 0;

  tok = strchr (arg, ',');
  num_of_escape = 0;

  /* make sure it is not escaped */
  while (tok)
    {
      if (*(tok-1) == '\\')
        {
          /* find the next one */
          tok = strchr (tok+1, ',');
          ++num_of_escape;
        }
      else
        break;
    }

  if (tok)
    len = (size_t)(tok - arg + 1);
  else
    len = strlen (arg) + 1;

  len -= num_of_escape;

  ret = (char *) malloc (len);

  i = 0;
  j = 0;
  while (arg[i] && (j < len-1))
    {
      if (arg[i] == '\\' && 
	  arg[ i + 1 ] && 
	  arg[ i + 1 ] == ',')
        ++i;

      ret[j++] = arg[i++];
    }

  ret[len-1] = '\0';

  return ret;
}

static const char *
get_multiple_arg_token_next(const char *arg)
{
  const char *tok;

  if (!arg)
    return 0;

  tok = strchr (arg, ',');

  /* make sure it is not escaped */
  while (tok)
    {
      if (*(tok-1) == '\\')
        {
          /* find the next one */
          tok = strchr (tok+1, ',');
        }
      else
        break;
    }

  if (! tok || strlen(tok) == 1)
    return 0;

  return tok+1;
}

static int
check_multiple_option_occurrences(const char *prog_name, unsigned int option_given, unsigned int min, unsigned int max, const char *option_desc);

int
check_multiple_option_occurrences(const char *prog_name, unsigned int option_given, unsigned int min, unsigned int max, const char *option_desc)
{
  int error_occurred = 0;

  if (option_given && (min > 0 || max > 0))
    {
      if (min > 0 && max > 0)
        {
          if (min == max)
            {
              /* specific occurrences */
              if (option_given != (unsigned int) min)
                {
                  fprintf (stderr, "%s: %s option occurrences must be %d\n",
                    prog_name, option_desc, min);
                  error_occurred = 1;
                }
            }
          else if (option_given < (unsigned int) min
                || option_given > (unsigned int) max)
            {
              /* range occurrences */
              fprintf (stderr, "%s: %s option occurrences must be between %d and %d\n",
                prog_name, option_desc, min, max);
              error_occurred = 1;
            }
        }
      else if (min > 0)
        {
          /* at least check */
          if (option_given < min)
            {
              fprintf (stderr, "%s: %s option occurrences must be at least %d\n",
                prog_name, option_desc, min);
              error_occurred = 1;
            }
        }
      else if (max > 0)
        {
          /* at most check */
          if (option_given > max)
            {
              fprintf (stderr, "%s: %s option occurrences must be at most %d\n",
                prog_name, option_desc, max);
              error_occurred = 1;
            }
        }
    }
    
  return error_occurred;
}
int
cmdline_parser (int argc, char **argv, struct gengetopt_args_info *args_info)
{
  return cmdline_parser2 (argc, argv, args_info, 0, 1, 1);
}

int
cmdline_parser_ext (int argc, char **argv, struct gengetopt_args_info *args_info,
                   struct cmdline_parser_params *params)
{
  int result;
  result = cmdline_parser_internal (argc, argv, args_info, params, 0);

  if (result == EXIT_FAILURE)
    {
      cmdline_parser_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
cmdline_parser2 (int argc, char **argv, struct gengetopt_args_info *args_info, int override, int initialize, int check_required)
{
  int result;
  struct cmdline_parser_params params;
  
  params.override = override;
  params.initialize = initialize;
  params.check_required = check_required;
  params.check_ambiguity = 0;
  params.print_errors = 1;

  result = cmdline_parser_internal (argc, argv, args_info, &params, 0);

  if (result == EXIT_FAILURE)
    {
      cmdline_parser_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
cmdline_parser_required (struct gengetopt_args_info *args_info, const char *prog_name)
{
  int result = EXIT_SUCCESS;

  if (cmdline_parser_required2(args_info, prog_name, 0) > 0)
    result = EXIT_FAILURE;

  if (result == EXIT_FAILURE)
    {
      cmdline_parser_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
cmdline_parser_required2 (struct gengetopt_args_info *args_info, const char *prog_name, const char *additional_error)
{
  int error_occurred = 0;
  FIX_UNUSED (additional_error);

  /* checks for required options */
  if (check_multiple_option_occurrences(prog_name, args_info->verbose_given, args_info->verbose_min, args_info->verbose_max, "'--verbose' ('-v')"))
     error_occurred = 1;
  
  if (check_multiple_option_occurrences(prog_name, args_info->source_given, args_info->source_min, args_info->source_max, "'--source' ('-s')"))
     error_occurred = 1;
  
  if (check_multiple_option_occurrences(prog_name, args_info->repair_given, args_info->repair_min, args_info->repair_max, "'--repair' ('-r')"))
     error_occurred = 1;
  
  if (check_multiple_option_occurrences(prog_name, args_info->control_given, args_info->control_min, args_info->control_max, "'--control' ('-c')"))
     error_occurred = 1;
  
  if (check_multiple_option_occurrences(prog_name, args_info->miface_given, args_info->miface_min, args_info->miface_max, "'--miface'"))
     error_occurred = 1;
  
  
  /* checks for dependences among options */

  return error_occurred;
}

/*
 * Extracted from the glibc source tree, version 2.3.6
 *
 * Licensed under the GPL as per the whole glibc source tree.
 *
 * This file was modified so that getopt_long can be called
 * many times without risking previous memory to be spoiled.
 *
 * Modified by Andre Noll and Lorenzo Bettini for use in
 * GNU gengetopt generated files.
 *
 */

/* 
 * we must include anything we need since this file is not thought to be
 * inserted in a file already using getopt.h
 *
 * Lorenzo
 */

struct option
{
  const char *name;
  /* has_arg can't be an enum because some compilers complain about
     type mismatches in all the code that assumes it is an int.  */
  int has_arg;
  int *flag;
  int val;
};

/* This version of `getopt' appears to the caller like standard Unix `getopt'
   but it behaves differently for the user, since it allows the user
   to intersperse the options with the other arguments.

   As `getopt' works, it permutes the elements of ARGV so that,
   when it is done, all the options precede everything else.  Thus
   all application programs are extended to handle flexible argument order.
*/
/*
   If the field `flag' is not NULL, it points to a variable that is set
   to the value given in the field `val' when the option is found, but
   left unchanged if the option is not found.

   To have a long-named option do something other than set an `int' to
   a compiled-in constant, such as set a value from `custom_optarg', set the
   option's `flag' field to zero and its `val' field to a nonzero
   value (the equivalent single-letter option character, if there is
   one).  For long options that have a zero `flag' field, `getopt'
   returns the contents of the `val' field.  */

/* Names for the values of the `has_arg' field of `struct option'.  */
#ifndef no_argument
#define no_argument		0
#endif

#ifndef required_argument
#define required_argument	1
#endif

#ifndef optional_argument
#define optional_argument	2
#endif

struct custom_getopt_data {
	/*
	 * These have exactly the same meaning as the corresponding global variables,
	 * except that they are used for the reentrant versions of getopt.
	 */
	int custom_optind;
	int custom_opterr;
	int custom_optopt;
	char *custom_optarg;

	/* True if the internal members have been initialized.  */
	int initialized;

	/*
	 * The next char to be scanned in the option-element in which the last option
	 * character we returned was found.  This allows us to pick up the scan where
	 * we left off.  If this is zero, or a null string, it means resume the scan by
	 * advancing to the next ARGV-element.
	 */
	char *nextchar;

	/*
	 * Describe the part of ARGV that contains non-options that have been skipped.
	 * `first_nonopt' is the index in ARGV of the first of them; `last_nonopt' is
	 * the index after the last of them.
	 */
	int first_nonopt;
	int last_nonopt;
};

/*
 * the variables optarg, optind, opterr and optopt are renamed with
 * the custom_ prefix so that they don't interfere with getopt ones.
 *
 * Moreover they're static so they are visible only from within the
 * file where this very file will be included.
 */

/*
 * For communication from `custom_getopt' to the caller.  When `custom_getopt' finds an
 * option that takes an argument, the argument value is returned here.
 */
static char *custom_optarg;

/*
 * Index in ARGV of the next element to be scanned.  This is used for
 * communication to and from the caller and for communication between
 * successive calls to `custom_getopt'.
 *
 * On entry to `custom_getopt', 1 means this is the first call; initialize.
 *
 * When `custom_getopt' returns -1, this is the index of the first of the non-option
 * elements that the caller should itself scan.
 *
 * Otherwise, `custom_optind' communicates from one call to the next how much of ARGV
 * has been scanned so far.
 *
 * 1003.2 says this must be 1 before any call.
 */
static int custom_optind = 1;

/*
 * Callers store zero here to inhibit the error message for unrecognized
 * options.
 */
static int custom_opterr = 1;

/*
 * Set to an option character which was unrecognized.  This must be initialized
 * on some systems to avoid linking in the system's own getopt implementation.
 */
static int custom_optopt = '?';

/*
 * Exchange two adjacent subsequences of ARGV.  One subsequence is elements
 * [first_nonopt,last_nonopt) which contains all the non-options that have been
 * skipped so far.  The other is elements [last_nonopt,custom_optind), which contains
 * all the options processed since those non-options were skipped.
 * `first_nonopt' and `last_nonopt' are relocated so that they describe the new
 * indices of the non-options in ARGV after they are moved.
 */
static void exchange(char **argv, struct custom_getopt_data *d)
{
	int bottom = d->first_nonopt;
	int middle = d->last_nonopt;
	int top = d->custom_optind;
	char *tem;

	/*
	 * Exchange the shorter segment with the far end of the longer segment.
	 * That puts the shorter segment into the right place.  It leaves the
	 * longer segment in the right place overall, but it consists of two
	 * parts that need to be swapped next.
	 */
	while (top > middle && middle > bottom) {
		if (top - middle > middle - bottom) {
			/* Bottom segment is the short one.  */
			int len = middle - bottom;
			int i;

			/* Swap it with the top part of the top segment.  */
			for (i = 0; i < len; i++) {
				tem = argv[bottom + i];
				argv[bottom + i] =
					argv[top - (middle - bottom) + i];
				argv[top - (middle - bottom) + i] = tem;
			}
			/* Exclude the moved bottom segment from further swapping.  */
			top -= len;
		} else {
			/* Top segment is the short one.  */
			int len = top - middle;
			int i;

			/* Swap it with the bottom part of the bottom segment.  */
			for (i = 0; i < len; i++) {
				tem = argv[bottom + i];
				argv[bottom + i] = argv[middle + i];
				argv[middle + i] = tem;
			}
			/* Exclude the moved top segment from further swapping.  */
			bottom += len;
		}
	}
	/* Update records for the slots the non-options now occupy.  */
	d->first_nonopt += (d->custom_optind - d->last_nonopt);
	d->last_nonopt = d->custom_optind;
}

/* Initialize the internal data when the first call is made.  */
static void custom_getopt_initialize(struct custom_getopt_data *d)
{
	/*
	 * Start processing options with ARGV-element 1 (since ARGV-element 0
	 * is the program name); the sequence of previously skipped non-option
	 * ARGV-elements is empty.
	 */
	d->first_nonopt = d->last_nonopt = d->custom_optind;
	d->nextchar = NULL;
	d->initialized = 1;
}

#define NONOPTION_P (argv[d->custom_optind][0] != '-' || argv[d->custom_optind][1] == '\0')

/* return: zero: continue, nonzero: return given value to user */
static int shuffle_argv(int argc, char *const *argv,const struct option *longopts,
	struct custom_getopt_data *d)
{
	/*
	 * Give FIRST_NONOPT & LAST_NONOPT rational values if CUSTOM_OPTIND has been
	 * moved back by the user (who may also have changed the arguments).
	 */
	if (d->last_nonopt > d->custom_optind)
		d->last_nonopt = d->custom_optind;
	if (d->first_nonopt > d->custom_optind)
		d->first_nonopt = d->custom_optind;
	/*
	 * If we have just processed some options following some
	 * non-options, exchange them so that the options come first.
	 */
	if (d->first_nonopt != d->last_nonopt &&
			d->last_nonopt != d->custom_optind)
		exchange((char **) argv, d);
	else if (d->last_nonopt != d->custom_optind)
		d->first_nonopt = d->custom_optind;
	/*
	 * Skip any additional non-options and extend the range of
	 * non-options previously skipped.
	 */
	while (d->custom_optind < argc && NONOPTION_P)
		d->custom_optind++;
	d->last_nonopt = d->custom_optind;
	/*
	 * The special ARGV-element `--' means premature end of options.  Skip
	 * it like a null option, then exchange with previous non-options as if
	 * it were an option, then skip everything else like a non-option.
	 */
	if (d->custom_optind != argc && !strcmp(argv[d->custom_optind], "--")) {
		d->custom_optind++;
		if (d->first_nonopt != d->last_nonopt
				&& d->last_nonopt != d->custom_optind)
			exchange((char **) argv, d);
		else if (d->first_nonopt == d->last_nonopt)
			d->first_nonopt = d->custom_optind;
		d->last_nonopt = argc;
		d->custom_optind = argc;
	}
	/*
	 * If we have done all the ARGV-elements, stop the scan and back over
	 * any non-options that we skipped and permuted.
	 */
	if (d->custom_optind == argc) {
		/*
		 * Set the next-arg-index to point at the non-options that we
		 * previously skipped, so the caller will digest them.
		 */
		if (d->first_nonopt != d->last_nonopt)
			d->custom_optind = d->first_nonopt;
		return -1;
	}
	/*
	 * If we have come to a non-option and did not permute it, either stop
	 * the scan or describe it to the caller and pass it by.
	 */
	if (NONOPTION_P) {
		d->custom_optarg = argv[d->custom_optind++];
		return 1;
	}
	/*
	 * We have found another option-ARGV-element. Skip the initial
	 * punctuation.
	 */
	d->nextchar = (argv[d->custom_optind] + 1 + (longopts != NULL && argv[d->custom_optind][1] == '-'));
	return 0;
}

/*
 * Check whether the ARGV-element is a long option.
 *
 * If there's a long option "fubar" and the ARGV-element is "-fu", consider
 * that an abbreviation of the long option, just like "--fu", and not "-f" with
 * arg "u".
 *
 * This distinction seems to be the most useful approach.
 *
 */
static int check_long_opt(int argc, char *const *argv, const char *optstring,
		const struct option *longopts, int *longind,
		int print_errors, struct custom_getopt_data *d)
{
	char *nameend;
	const struct option *p;
	const struct option *pfound = NULL;
	int exact = 0;
	int ambig = 0;
	int indfound = -1;
	int option_index;

	for (nameend = d->nextchar; *nameend && *nameend != '='; nameend++)
		/* Do nothing.  */ ;

	/* Test all long options for either exact match or abbreviated matches */
	for (p = longopts, option_index = 0; p->name; p++, option_index++)
		if (!strncmp(p->name, d->nextchar, nameend - d->nextchar)) {
			if ((unsigned int) (nameend - d->nextchar)
					== (unsigned int) strlen(p->name)) {
				/* Exact match found.  */
				pfound = p;
				indfound = option_index;
				exact = 1;
				break;
			} else if (pfound == NULL) {
				/* First nonexact match found.  */
				pfound = p;
				indfound = option_index;
			} else if (pfound->has_arg != p->has_arg
					|| pfound->flag != p->flag
					|| pfound->val != p->val)
				/* Second or later nonexact match found.  */
				ambig = 1;
		}
	if (ambig && !exact) {
		if (print_errors) {
			fprintf(stderr,
				"%s: option `%s' is ambiguous\n",
				argv[0], argv[d->custom_optind]);
		}
		d->nextchar += strlen(d->nextchar);
		d->custom_optind++;
		d->custom_optopt = 0;
		return '?';
	}
	if (pfound) {
		option_index = indfound;
		d->custom_optind++;
		if (*nameend) {
			if (pfound->has_arg != no_argument)
				d->custom_optarg = nameend + 1;
			else {
				if (print_errors) {
					if (argv[d->custom_optind - 1][1] == '-') {
						/* --option */
						fprintf(stderr, "%s: option `--%s' doesn't allow an argument\n",
							argv[0], pfound->name);
					} else {
						/* +option or -option */
						fprintf(stderr, "%s: option `%c%s' doesn't allow an argument\n",
							argv[0], argv[d->custom_optind - 1][0], pfound->name);
					}

				}
				d->nextchar += strlen(d->nextchar);
				d->custom_optopt = pfound->val;
				return '?';
			}
		} else if (pfound->has_arg == required_argument) {
			if (d->custom_optind < argc)
				d->custom_optarg = argv[d->custom_optind++];
			else {
				if (print_errors) {
					fprintf(stderr,
						"%s: option `%s' requires an argument\n",
						argv[0],
						argv[d->custom_optind - 1]);
				}
				d->nextchar += strlen(d->nextchar);
				d->custom_optopt = pfound->val;
				return optstring[0] == ':' ? ':' : '?';
			}
		}
		d->nextchar += strlen(d->nextchar);
		if (longind != NULL)
			*longind = option_index;
		if (pfound->flag) {
			*(pfound->flag) = pfound->val;
			return 0;
		}
		return pfound->val;
	}
	/*
	 * Can't find it as a long option.  If this is not getopt_long_only, or
	 * the option starts with '--' or is not a valid short option, then
	 * it's an error.  Otherwise interpret it as a short option.
	 */
	if (print_errors) {
		if (argv[d->custom_optind][1] == '-') {
			/* --option */
			fprintf(stderr,
				"%s: unrecognized option `--%s'\n",
				argv[0], d->nextchar);
		} else {
			/* +option or -option */
			fprintf(stderr,
				"%s: unrecognized option `%c%s'\n",
				argv[0], argv[d->custom_optind][0],
				d->nextchar);
		}
	}
	d->nextchar = (char *) "";
	d->custom_optind++;
	d->custom_optopt = 0;
	return '?';
}

static int check_short_opt(int argc, char *const *argv, const char *optstring,
		int print_errors, struct custom_getopt_data *d)
{
	char c = *d->nextchar++;
	const char *temp = strchr(optstring, c);

	/* Increment `custom_optind' when we start to process its last character.  */
	if (*d->nextchar == '\0')
		++d->custom_optind;
	if (!temp || c == ':') {
		if (print_errors)
			fprintf(stderr, "%s: invalid option -- %c\n", argv[0], c);

		d->custom_optopt = c;
		return '?';
	}
	if (temp[1] == ':') {
		if (temp[2] == ':') {
			/* This is an option that accepts an argument optionally.  */
			if (*d->nextchar != '\0') {
				d->custom_optarg = d->nextchar;
				d->custom_optind++;
			} else
				d->custom_optarg = NULL;
			d->nextchar = NULL;
		} else {
			/* This is an option that requires an argument.  */
			if (*d->nextchar != '\0') {
				d->custom_optarg = d->nextchar;
				/*
				 * If we end this ARGV-element by taking the
				 * rest as an arg, we must advance to the next
				 * element now.
				 */
				d->custom_optind++;
			} else if (d->custom_optind == argc) {
				if (print_errors) {
					fprintf(stderr,
						"%s: option requires an argument -- %c\n",
						argv[0], c);
				}
				d->custom_optopt = c;
				if (optstring[0] == ':')
					c = ':';
				else
					c = '?';
			} else
				/*
				 * We already incremented `custom_optind' once;
				 * increment it again when taking next ARGV-elt
				 * as argument.
				 */
				d->custom_optarg = argv[d->custom_optind++];
			d->nextchar = NULL;
		}
	}
	return c;
}

/*
 * Scan elements of ARGV for option characters given in OPTSTRING.
 *
 * If an element of ARGV starts with '-', and is not exactly "-" or "--",
 * then it is an option element.  The characters of this element
 * (aside from the initial '-') are option characters.  If `getopt'
 * is called repeatedly, it returns successively each of the option characters
 * from each of the option elements.
 *
 * If `getopt' finds another option character, it returns that character,
 * updating `custom_optind' and `nextchar' so that the next call to `getopt' can
 * resume the scan with the following option character or ARGV-element.
 *
 * If there are no more option characters, `getopt' returns -1.
 * Then `custom_optind' is the index in ARGV of the first ARGV-element
 * that is not an option.  (The ARGV-elements have been permuted
 * so that those that are not options now come last.)
 *
 * OPTSTRING is a string containing the legitimate option characters.
 * If an option character is seen that is not listed in OPTSTRING,
 * return '?' after printing an error message.  If you set `custom_opterr' to
 * zero, the error message is suppressed but we still return '?'.
 *
 * If a char in OPTSTRING is followed by a colon, that means it wants an arg,
 * so the following text in the same ARGV-element, or the text of the following
 * ARGV-element, is returned in `custom_optarg'.  Two colons mean an option that
 * wants an optional arg; if there is text in the current ARGV-element,
 * it is returned in `custom_optarg', otherwise `custom_optarg' is set to zero.
 *
 * If OPTSTRING starts with `-' or `+', it requests different methods of
 * handling the non-option ARGV-elements.
 * See the comments about RETURN_IN_ORDER and REQUIRE_ORDER, above.
 *
 * Long-named options begin with `--' instead of `-'.
 * Their names may be abbreviated as long as the abbreviation is unique
 * or is an exact match for some defined option.  If they have an
 * argument, it follows the option name in the same ARGV-element, separated
 * from the option name by a `=', or else the in next ARGV-element.
 * When `getopt' finds a long-named option, it returns 0 if that option's
 * `flag' field is nonzero, the value of the option's `val' field
 * if the `flag' field is zero.
 *
 * The elements of ARGV aren't really const, because we permute them.
 * But we pretend they're const in the prototype to be compatible
 * with other systems.
 *
 * LONGOPTS is a vector of `struct option' terminated by an
 * element containing a name which is zero.
 *
 * LONGIND returns the index in LONGOPT of the long-named option found.
 * It is only valid when a long-named option has been found by the most
 * recent call.
 *
 * Return the option character from OPTS just read.  Return -1 when there are
 * no more options.  For unrecognized options, or options missing arguments,
 * `custom_optopt' is set to the option letter, and '?' is returned.
 *
 * The OPTS string is a list of characters which are recognized option letters,
 * optionally followed by colons, specifying that that letter takes an
 * argument, to be placed in `custom_optarg'.
 *
 * If a letter in OPTS is followed by two colons, its argument is optional.
 * This behavior is specific to the GNU `getopt'.
 *
 * The argument `--' causes premature termination of argument scanning,
 * explicitly telling `getopt' that there are no more options.  If OPTS begins
 * with `--', then non-option arguments are treated as arguments to the option
 * '\0'.  This behavior is specific to the GNU `getopt'.
 */

static int getopt_internal_r(int argc, char *const *argv, const char *optstring,
		const struct option *longopts, int *longind,
		struct custom_getopt_data *d)
{
	int ret, print_errors = d->custom_opterr;

	if (optstring[0] == ':')
		print_errors = 0;
	if (argc < 1)
		return -1;
	d->custom_optarg = NULL;

	/* 
	 * This is a big difference with GNU getopt, since optind == 0
	 * means initialization while here 1 means first call.
	 */
	if (d->custom_optind == 0 || !d->initialized) {
		if (d->custom_optind == 0)
			d->custom_optind = 1;	/* Don't scan ARGV[0], the program name.  */
		custom_getopt_initialize(d);
	}
	if (d->nextchar == NULL || *d->nextchar == '\0') {
		ret = shuffle_argv(argc, argv, longopts, d);
		if (ret)
			return ret;
	}
	if (longopts && (argv[d->custom_optind][1] == '-' ))
		return check_long_opt(argc, argv, optstring, longopts,
			longind, print_errors, d);
	return check_short_opt(argc, argv, optstring, print_errors, d);
}

static int custom_getopt_internal(int argc, char *const *argv, const char *optstring,
	const struct option *longopts, int *longind)
{
	int result;
	/* Keep a global copy of all internal members of d */
	static struct custom_getopt_data d;

	d.custom_optind = custom_optind;
	d.custom_opterr = custom_opterr;
	result = getopt_internal_r(argc, argv, optstring, longopts,
		longind, &d);
	custom_optind = d.custom_optind;
	custom_optarg = d.custom_optarg;
	custom_optopt = d.custom_optopt;
	return result;
}

static int custom_getopt_long (int argc, char *const *argv, const char *options,
	const struct option *long_options, int *opt_index)
{
	return custom_getopt_internal(argc, argv, options, long_options,
		opt_index);
}


static char *package_name = 0;

/**
 * @brief updates an option
 * @param field the generic pointer to the field to update
 * @param orig_field the pointer to the orig field
 * @param field_given the pointer to the number of occurrence of this option
 * @param prev_given the pointer to the number of occurrence already seen
 * @param value the argument for this option (if null no arg was specified)
 * @param possible_values the possible values for this option (if specified)
 * @param default_value the default value (in case the option only accepts fixed values)
 * @param arg_type the type of this option
 * @param check_ambiguity @see cmdline_parser_params.check_ambiguity
 * @param override @see cmdline_parser_params.override
 * @param no_free whether to free a possible previous value
 * @param multiple_option whether this is a multiple option
 * @param long_opt the corresponding long option
 * @param short_opt the corresponding short option (or '-' if none)
 * @param additional_error possible further error specification
 */
static
int update_arg(void *field, char **orig_field,
               unsigned int *field_given, unsigned int *prev_given, 
               char *value, const char *possible_values[],
               const char *default_value,
               cmdline_parser_arg_type arg_type,
               int check_ambiguity, int override,
               int no_free, int multiple_option,
               const char *long_opt, char short_opt,
               const char *additional_error)
{
  char *stop_char = 0;
  const char *val = value;
  int found;
  char **string_field;
  FIX_UNUSED (field);

  stop_char = 0;
  found = 0;

  if (!multiple_option && prev_given && (*prev_given || (check_ambiguity && *field_given)))
    {
      if (short_opt != '-')
        fprintf (stderr, "%s: `--%s' (`-%c') option given more than once%s\n", 
               package_name, long_opt, short_opt,
               (additional_error ? additional_error : ""));
      else
        fprintf (stderr, "%s: `--%s' option given more than once%s\n", 
               package_name, long_opt,
               (additional_error ? additional_error : ""));
      return 1; /* failure */
    }

  if (possible_values && (found = check_possible_values((value ? value : default_value), possible_values)) < 0)
    {
      if (short_opt != '-')
        fprintf (stderr, "%s: %s argument, \"%s\", for option `--%s' (`-%c')%s\n", 
          package_name, (found == -2) ? "ambiguous" : "invalid", value, long_opt, short_opt,
          (additional_error ? additional_error : ""));
      else
        fprintf (stderr, "%s: %s argument, \"%s\", for option `--%s'%s\n", 
          package_name, (found == -2) ? "ambiguous" : "invalid", value, long_opt,
          (additional_error ? additional_error : ""));
      return 1; /* failure */
    }
    
  if (field_given && *field_given && ! override)
    return 0;
  if (prev_given)
    (*prev_given)++;
  if (field_given)
    (*field_given)++;
  if (possible_values)
    val = possible_values[found];

  switch(arg_type) {
  case ARG_FLAG:
    *((int *)field) = !*((int *)field);
    break;
  case ARG_INT:
    if (val) *((int *)field) = strtol (val, &stop_char, 0);
    break;
  case ARG_ENUM:
    if (val) *((int *)field) = found;
    break;
  case ARG_STRING:
    if (val) {
      string_field = (char **)field;
      if (!no_free && *string_field)
        free (*string_field); /* free previous string */
      *string_field = gengetopt_strdup (val);
    }
    break;
  default:
    break;
  };

  /* check numeric conversion */
  switch(arg_type) {
  case ARG_INT:
    if (val && !(stop_char && *stop_char == '\0')) {
      fprintf(stderr, "%s: invalid numeric value: %s\n", package_name, val);
      return 1; /* failure */
    }
    break;
  default:
    ;
  };

  /* store the original value */
  switch(arg_type) {
  case ARG_NO:
  case ARG_FLAG:
    break;
  default:
    if (value && orig_field) {
      if (no_free) {
        *orig_field = value;
      } else {
        if (*orig_field)
          free (*orig_field); /* free previous string */
        *orig_field = gengetopt_strdup (value);
      }
    }
  };

  return 0; /* OK */
}

/**
 * @brief store information about a multiple option in a temporary list
 * @param list where to (temporarily) store multiple options
 */
static
int update_multiple_arg_temp(struct generic_list **list,
               unsigned int *prev_given, const char *val,
               const char *possible_values[], const char *default_value,
               cmdline_parser_arg_type arg_type,
               const char *long_opt, char short_opt,
               const char *additional_error)
{
  /* store single arguments */
  char *multi_token;
  const char *multi_next;

  if (arg_type == ARG_NO) {
    (*prev_given)++;
    return 0; /* OK */
  }

  multi_token = get_multiple_arg_token(val);
  multi_next = get_multiple_arg_token_next (val);

  while (1)
    {
      add_node (list);
      if (update_arg((void *)&((*list)->arg), &((*list)->orig), 0,
          prev_given, multi_token, possible_values, default_value, 
          arg_type, 0, 1, 1, 1, long_opt, short_opt, additional_error)) {
        if (multi_token) free(multi_token);
        return 1; /* failure */
      }

      if (multi_next)
        {
          multi_token = get_multiple_arg_token(multi_next);
          multi_next = get_multiple_arg_token_next (multi_next);
        }
      else
        break;
    }

  return 0; /* OK */
}

/**
 * @brief free the passed list (including possible string argument)
 */
static
void free_list(struct generic_list *list, short string_arg)
{
  if (list) {
    struct generic_list *tmp;
    while (list)
      {
        tmp = list;
        if (string_arg && list->arg.string_arg)
          free (list->arg.string_arg);
        if (list->orig)
          free (list->orig);
        list = list->next;
        free (tmp);
      }
  }
}

/**
 * @brief updates a multiple option starting from the passed list
 */
static
void update_multiple_arg(void *field, char ***orig_field,
               unsigned int field_given, unsigned int prev_given, union generic_value *default_value,
               cmdline_parser_arg_type arg_type,
               struct generic_list *list)
{
  int i;
  struct generic_list *tmp;

  if (prev_given && list) {
    *orig_field = (char **) realloc (*orig_field, (field_given + prev_given) * sizeof (char *));

    switch(arg_type) {
    case ARG_INT:
    case ARG_ENUM:
      *((int **)field) = (int *)realloc (*((int **)field), (field_given + prev_given) * sizeof (int)); break;
    case ARG_STRING:
      *((char ***)field) = (char **)realloc (*((char ***)field), (field_given + prev_given) * sizeof (char *)); break;
    default:
      break;
    };
    
    for (i = (prev_given - 1); i >= 0; --i)
      {
        tmp = list;
        
        switch(arg_type) {
        case ARG_INT:
          (*((int **)field))[i + field_given] = tmp->arg.int_arg; break;
        case ARG_ENUM:
          (*((int **)field))[i + field_given] = tmp->arg.int_arg; break;
        case ARG_STRING:
          (*((char ***)field))[i + field_given] = tmp->arg.string_arg; break;
        default:
          break;
        }        
        (*orig_field) [i + field_given] = list->orig;
        list = list->next;
        free (tmp);
      }
  } else { /* set the default value */
    if (default_value && ! field_given) {
      switch(arg_type) {
      case ARG_INT:
      case ARG_ENUM:
        if (! *((int **)field)) {
          *((int **)field) = (int *)malloc (sizeof (int));
          (*((int **)field))[0] = default_value->int_arg; 
        }
        break;
      case ARG_STRING:
        if (! *((char ***)field)) {
          *((char ***)field) = (char **)malloc (sizeof (char *));
          (*((char ***)field))[0] = gengetopt_strdup(default_value->string_arg);
        }
        break;
      default: break;
      }
      if (!(*orig_field)) {
        *orig_field = (char **) malloc (sizeof (char *));
        (*orig_field)[0] = 0;
      }
    }
  }
}

int
cmdline_parser_internal (
  int argc, char **argv, struct gengetopt_args_info *args_info,
                        struct cmdline_parser_params *params, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  struct generic_list * source_list = NULL;
  struct generic_list * repair_list = NULL;
  struct generic_list * control_list = NULL;
  struct generic_list * miface_list = NULL;
  int error_occurred = 0;
  struct gengetopt_args_info local_args_info;
  
  int override;
  int initialize;
  int check_required;
  int check_ambiguity;

  char *optarg;
  int optind;
  int opterr;
  int optopt;
  
  package_name = argv[0];
  
  /* TODO: Why is this here? It is not used anywhere. */
  override = params->override;
  FIX_UNUSED(override);

  initialize = params->initialize;
  check_required = params->check_required;

  /* TODO: Why is this here? It is not used anywhere. */
  check_ambiguity = params->check_ambiguity;
  FIX_UNUSED(check_ambiguity);

  if (initialize)
    cmdline_parser_init (args_info);

  cmdline_parser_init (&local_args_info);

  optarg = 0;
  optind = 0;
  opterr = params->print_errors;
  optopt = '?';

  while (1)
    {
      int option_index = 0;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "verbose",	0, NULL, 'v' },
        { "color",	1, NULL, 0 },
        { "list-supported",	0, NULL, 'L' },
        { "input",	1, NULL, 'i' },
        { "io-encoding",	1, NULL, 0 },
        { "io-latency",	1, NULL, 0 },
        { "io-frame-len",	1, NULL, 0 },
        { "source",	1, NULL, 's' },
        { "repair",	1, NULL, 'r' },
        { "control",	1, NULL, 'c' },
        { "miface",	1, NULL, 0 },
        { "reuseaddr",	0, NULL, 0 },
        { "packet-encoding",	1, NULL, 0 },
        { "packet-len",	1, NULL, 0 },
        { "fec-encoding",	1, NULL, 0 },
        { "fec-block-src",	1, NULL, 0 },
        { "fec-block-rpr",	1, NULL, 0 },
        { "latency-backend",	1, NULL, 0 },
        { "latency-profile",	1, NULL, 0 },
        { "resampler-backend",	1, NULL, 0 },
        { "resampler-profile",	1, NULL, 0 },
        { "target-latency",	1, NULL, 0 },
        { "latency-tolerance",	1, NULL, 0 },
        { "start-latency",	1, NULL, 0 },
        { "min-latency",	1, NULL, 0 },
        { "max-latency",	1, NULL, 0 },
        { "auto-cts",	0, NULL, 0 },
        { "prometheus-metrics-port",	1, NULL, 0 },
        { "prometheus-niq-latency-buckets",	1, NULL, 0 },
        { "prometheus-niq-latency-min",	1, NULL, 0 },
        { "prometheus-niq-latency-max",	1, NULL, 0 },
        { "prometheus-niq-latency-scale",	1, NULL, 0 },
        { "prometheus-e2e-latency-buckets",	1, NULL, 0 },
        { "prometheus-e2e-latency-min",	1, NULL, 0 },
        { "prometheus-e2e-latency-max",	1, NULL, 0 },
        { "prometheus-e2e-latency-scale",	1, NULL, 0 },
        { "prometheus-jitter-buckets",	1, NULL, 0 },
        { "prometheus-jitter-min",	1, NULL, 0 },
        { "prometheus-jitter-max",	1, NULL, 0 },
        { "prometheus-jitter-scale",	1, NULL, 0 },
        { "prometheus-rtt-buckets",	1, NULL, 0 },
        { "prometheus-rtt-min",	1, NULL, 0 },
        { "prometheus-rtt-max",	1, NULL, 0 },
        { "prometheus-rtt-scale",	1, NULL, 0 },
        { "max-packet-size",	1, NULL, 0 },
        { "max-frame-size",	1, NULL, 0 },
        { "prof",	0, NULL, 0 },
        { "dump",	1, NULL, 0 },
        { 0,  0, 0, 0 }
      };

      custom_optarg = optarg;
      custom_optind = optind;
      custom_opterr = opterr;
      custom_optopt = optopt;

      c = custom_getopt_long (argc, argv, "hVvLi:s:r:c:", long_options, &option_index);

      optarg = custom_optarg;
      optind = custom_optind;
      opterr = custom_opterr;
      optopt = custom_optopt;

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          cmdline_parser_print_help ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          cmdline_parser_print_version ();
          cmdline_parser_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'v':	/* Increase verbosity level (may be used multiple times).  */
        
          local_args_info.verbose_given++;
        
          break;
        case 'L':	/* List supported protocols, formats, etc..  */
        
        
          if (update_arg( 0 , 
               0 , &(args_info->list_supported_given),
              &(local_args_info.list_supported_given), optarg, 0, 0, ARG_NO,
              check_ambiguity, override, 0, 0,
              "list-supported", 'L',
              additional_error))
            goto failure;
        
          break;
        case 'i':	/* Input file or device URI.  */
        
        
          if (update_arg( (void *)&(args_info->input_arg), 
               &(args_info->input_orig), &(args_info->input_given),
              &(local_args_info.input_given), optarg, 0, 0, ARG_STRING,
              check_ambiguity, override, 0, 0,
              "input", 'i',
              additional_error))
            goto failure;
        
          break;
        case 's':	/* Remote source endpoint to connect to.  */
        
          if (update_multiple_arg_temp(&source_list, 
              &(local_args_info.source_given), optarg, 0, 0, ARG_STRING,
              "source", 's',
              additional_error))
            goto failure;
        
          break;
        case 'r':	/* Remote repair endpoint to connect to.  */
        
          if (update_multiple_arg_temp(&repair_list, 
              &(local_args_info.repair_given), optarg, 0, 0, ARG_STRING,
              "repair", 'r',
              additional_error))
            goto failure;
        
          break;
        case 'c':	/* Remote control endpoint to connect to.  */
        
          if (update_multiple_arg_temp(&control_list, 
              &(local_args_info.control_given), optarg, 0, 0, ARG_STRING,
              "control", 'c',
              additional_error))
            goto failure;
        
          break;

        case 0:	/* Long option with no short option */
          /* Set colored logging mode for stderr output.  */
          if (strcmp (long_options[option_index].name, "color") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->color_arg), 
                 &(args_info->color_orig), &(args_info->color_given),
                &(local_args_info.color_given), optarg, cmdline_parser_color_values, "auto", ARG_ENUM,
                check_ambiguity, override, 0, 0,
                "color", '-',
                additional_error))
              goto failure;
          
          }
          /* Input device encoding.  */
          else if (strcmp (long_options[option_index].name, "io-encoding") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->io_encoding_arg), 
                 &(args_info->io_encoding_orig), &(args_info->io_encoding_given),
                &(local_args_info.io_encoding_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "io-encoding", '-',
                additional_error))
              goto failure;
          
          }
          /* Input device latency, TIME units.  */
          else if (strcmp (long_options[option_index].name, "io-latency") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->io_latency_arg), 
                 &(args_info->io_latency_orig), &(args_info->io_latency_given),
                &(local_args_info.io_latency_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "io-latency", '-',
                additional_error))
              goto failure;
          
          }
          /* Input frame length, TIME units.  */
          else if (strcmp (long_options[option_index].name, "io-frame-len") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->io_frame_len_arg), 
                 &(args_info->io_frame_len_orig), &(args_info->io_frame_len_given),
                &(local_args_info.io_frame_len_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "io-frame-len", '-',
                additional_error))
              goto failure;
          
          }
          /* IPv4 or IPv6 address of the network interface on which to join the multicast group.  */
          else if (strcmp (long_options[option_index].name, "miface") == 0)
          {
          
            if (update_multiple_arg_temp(&miface_list, 
                &(local_args_info.miface_given), optarg, 0, 0, ARG_STRING,
                "miface", '-',
                additional_error))
              goto failure;
          
          }
          /* Enable SO_REUSEADDR when binding sockets.  */
          else if (strcmp (long_options[option_index].name, "reuseaddr") == 0)
          {
          
          
            if (update_arg( 0 , 
                 0 , &(args_info->reuseaddr_given),
                &(local_args_info.reuseaddr_given), optarg, 0, 0, ARG_NO,
                check_ambiguity, override, 0, 0,
                "reuseaddr", '-',
                additional_error))
              goto failure;
          
          }
          /* Custom network packet encoding.  */
          else if (strcmp (long_options[option_index].name, "packet-encoding") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->packet_encoding_arg), 
                 &(args_info->packet_encoding_orig), &(args_info->packet_encoding_given),
                &(local_args_info.packet_encoding_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "packet-encoding", '-',
                additional_error))
              goto failure;
          
          }
          /* Network packet length, TIME units.  */
          else if (strcmp (long_options[option_index].name, "packet-len") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->packet_len_arg), 
                 &(args_info->packet_len_orig), &(args_info->packet_len_given),
                &(local_args_info.packet_len_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "packet-len", '-',
                additional_error))
              goto failure;
          
          }
          /* FEC encoding, 'auto' to auto-detect from network endpoints.  */
          else if (strcmp (long_options[option_index].name, "fec-encoding") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->fec_encoding_arg), 
                 &(args_info->fec_encoding_orig), &(args_info->fec_encoding_given),
                &(local_args_info.fec_encoding_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "fec-encoding", '-',
                additional_error))
              goto failure;
          
          }
          /* Number of source packets in FEC block.  */
          else if (strcmp (long_options[option_index].name, "fec-block-src") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->fec_block_src_arg), 
                 &(args_info->fec_block_src_orig), &(args_info->fec_block_src_given),
                &(local_args_info.fec_block_src_given), optarg, 0, 0, ARG_INT,
                check_ambiguity, override, 0, 0,
                "fec-block-src", '-',
                additional_error))
              goto failure;
          
          }
          /* Number of repair packets in FEC block.  */
          else if (strcmp (long_options[option_index].name, "fec-block-rpr") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->fec_block_rpr_arg), 
                 &(args_info->fec_block_rpr_orig), &(args_info->fec_block_rpr_given),
                &(local_args_info.fec_block_rpr_given), optarg, 0, 0, ARG_INT,
                check_ambiguity, override, 0, 0,
                "fec-block-rpr", '-',
                additional_error))
              goto failure;
          
          }
          /* Which latency to use in latency tuner.  */
          else if (strcmp (long_options[option_index].name, "latency-backend") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->latency_backend_arg), 
                 &(args_info->latency_backend_orig), &(args_info->latency_backend_given),
                &(local_args_info.latency_backend_given), optarg, cmdline_parser_latency_backend_values, "niq", ARG_ENUM,
                check_ambiguity, override, 0, 0,
                "latency-backend", '-',
                additional_error))
              goto failure;
          
          }
          /* Latency tuning profile.  */
          else if (strcmp (long_options[option_index].name, "latency-profile") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->latency_profile_arg), 
                 &(args_info->latency_profile_orig), &(args_info->latency_profile_given),
                &(local_args_info.latency_profile_given), optarg, cmdline_parser_latency_profile_values, "intact", ARG_ENUM,
                check_ambiguity, override, 0, 0,
                "latency-profile", '-',
                additional_error))
              goto failure;
          
          }
          /* Resampler backend.  */
          else if (strcmp (long_options[option_index].name, "resampler-backend") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->resampler_backend_arg), 
                 &(args_info->resampler_backend_orig), &(args_info->resampler_backend_given),
                &(local_args_info.resampler_backend_given), optarg, cmdline_parser_resampler_backend_values, "auto", ARG_ENUM,
                check_ambiguity, override, 0, 0,
                "resampler-backend", '-',
                additional_error))
              goto failure;
          
          }
          /* Resampler profile.  */
          else if (strcmp (long_options[option_index].name, "resampler-profile") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->resampler_profile_arg), 
                 &(args_info->resampler_profile_orig), &(args_info->resampler_profile_given),
                &(local_args_info.resampler_profile_given), optarg, cmdline_parser_resampler_profile_values, "medium", ARG_ENUM,
                check_ambiguity, override, 0, 0,
                "resampler-profile", '-',
                additional_error))
              goto failure;
          
          }
          /* Target latency, TIME units or 'auto' for adaptive mode.  */
          else if (strcmp (long_options[option_index].name, "target-latency") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->target_latency_arg), 
                 &(args_info->target_latency_orig), &(args_info->target_latency_given),
                &(local_args_info.target_latency_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "target-latency", '-',
                additional_error))
              goto failure;
          
          }
          /* Maximum deviation from target latency, TIME units.  */
          else if (strcmp (long_options[option_index].name, "latency-tolerance") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->latency_tolerance_arg), 
                 &(args_info->latency_tolerance_orig), &(args_info->latency_tolerance_given),
                &(local_args_info.latency_tolerance_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "latency-tolerance", '-',
                additional_error))
              goto failure;
          
          }
          /* Starting target latency in adaptive mode, TIME units.  */
          else if (strcmp (long_options[option_index].name, "start-latency") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->start_latency_arg), 
                 &(args_info->start_latency_orig), &(args_info->start_latency_given),
                &(local_args_info.start_latency_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "start-latency", '-',
                additional_error))
              goto failure;
          
          }
          /* Minimum target latency in adaptive mode, TIME units.  */
          else if (strcmp (long_options[option_index].name, "min-latency") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->min_latency_arg), 
                 &(args_info->min_latency_orig), &(args_info->min_latency_given),
                &(local_args_info.min_latency_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "min-latency", '-',
                additional_error))
              goto failure;
          
          }
          /* Maximum target latency in adaptive mode, TIME units.  */
          else if (strcmp (long_options[option_index].name, "max-latency") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->max_latency_arg), 
                 &(args_info->max_latency_orig), &(args_info->max_latency_given),
                &(local_args_info.max_latency_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "max-latency", '-',
                additional_error))
              goto failure;
          
          }
          /* Automatically stamp frames with capture timestamps (required for E2E latency).  */
          else if (strcmp (long_options[option_index].name, "auto-cts") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->auto_cts_flag), 0, &(args_info->auto_cts_given),
                &(local_args_info.auto_cts_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "auto-cts", '-',
                additional_error))
              goto failure;
          
          }
          /* Port for prometheus metrics HTTP exposer.  */
          else if (strcmp (long_options[option_index].name, "prometheus-metrics-port") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_metrics_port_arg), 
                 &(args_info->prometheus_metrics_port_orig), &(args_info->prometheus_metrics_port_given),
                &(local_args_info.prometheus_metrics_port_given), optarg, 0, "0", ARG_INT,
                check_ambiguity, override, 0, 0,
                "prometheus-metrics-port", '-',
                additional_error))
              goto failure;
          
          }
          /* Number of histogram buckets for NIQ latency metrics.  */
          else if (strcmp (long_options[option_index].name, "prometheus-niq-latency-buckets") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_niq_latency_buckets_arg), 
                 &(args_info->prometheus_niq_latency_buckets_orig), &(args_info->prometheus_niq_latency_buckets_given),
                &(local_args_info.prometheus_niq_latency_buckets_given), optarg, 0, "100", ARG_INT,
                check_ambiguity, override, 0, 0,
                "prometheus-niq-latency-buckets", '-',
                additional_error))
              goto failure;
          
          }
          /* Minimum NIQ latency bucket boundary, TIME units.  */
          else if (strcmp (long_options[option_index].name, "prometheus-niq-latency-min") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_niq_latency_min_arg), 
                 &(args_info->prometheus_niq_latency_min_orig), &(args_info->prometheus_niq_latency_min_given),
                &(local_args_info.prometheus_niq_latency_min_given), optarg, 0, "5ms", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "prometheus-niq-latency-min", '-',
                additional_error))
              goto failure;
          
          }
          /* Maximum NIQ latency bucket boundary, TIME units.  */
          else if (strcmp (long_options[option_index].name, "prometheus-niq-latency-max") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_niq_latency_max_arg), 
                 &(args_info->prometheus_niq_latency_max_orig), &(args_info->prometheus_niq_latency_max_given),
                &(local_args_info.prometheus_niq_latency_max_given), optarg, 0, "50ms", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "prometheus-niq-latency-max", '-',
                additional_error))
              goto failure;
          
          }
          /* Bucket spacing for NIQ latency histogram.  */
          else if (strcmp (long_options[option_index].name, "prometheus-niq-latency-scale") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_niq_latency_scale_arg), 
                 &(args_info->prometheus_niq_latency_scale_orig), &(args_info->prometheus_niq_latency_scale_given),
                &(local_args_info.prometheus_niq_latency_scale_given), optarg, cmdline_parser_prometheus_niq_latency_scale_values, "log", ARG_ENUM,
                check_ambiguity, override, 0, 0,
                "prometheus-niq-latency-scale", '-',
                additional_error))
              goto failure;
          
          }
          /* Number of histogram buckets for E2E latency metrics.  */
          else if (strcmp (long_options[option_index].name, "prometheus-e2e-latency-buckets") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_e2e_latency_buckets_arg), 
                 &(args_info->prometheus_e2e_latency_buckets_orig), &(args_info->prometheus_e2e_latency_buckets_given),
                &(local_args_info.prometheus_e2e_latency_buckets_given), optarg, 0, "100", ARG_INT,
                check_ambiguity, override, 0, 0,
                "prometheus-e2e-latency-buckets", '-',
                additional_error))
              goto failure;
          
          }
          /* Minimum E2E latency bucket boundary, TIME units.  */
          else if (strcmp (long_options[option_index].name, "prometheus-e2e-latency-min") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_e2e_latency_min_arg), 
                 &(args_info->prometheus_e2e_latency_min_orig), &(args_info->prometheus_e2e_latency_min_given),
                &(local_args_info.prometheus_e2e_latency_min_given), optarg, 0, "20ms", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "prometheus-e2e-latency-min", '-',
                additional_error))
              goto failure;
          
          }
          /* Maximum E2E latency bucket boundary, TIME units.  */
          else if (strcmp (long_options[option_index].name, "prometheus-e2e-latency-max") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_e2e_latency_max_arg), 
                 &(args_info->prometheus_e2e_latency_max_orig), &(args_info->prometheus_e2e_latency_max_given),
                &(local_args_info.prometheus_e2e_latency_max_given), optarg, 0, "200ms", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "prometheus-e2e-latency-max", '-',
                additional_error))
              goto failure;
          
          }
          /* Bucket spacing for E2E latency histogram.  */
          else if (strcmp (long_options[option_index].name, "prometheus-e2e-latency-scale") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_e2e_latency_scale_arg), 
                 &(args_info->prometheus_e2e_latency_scale_orig), &(args_info->prometheus_e2e_latency_scale_given),
                &(local_args_info.prometheus_e2e_latency_scale_given), optarg, cmdline_parser_prometheus_e2e_latency_scale_values, "log", ARG_ENUM,
                check_ambiguity, override, 0, 0,
                "prometheus-e2e-latency-scale", '-',
                additional_error))
              goto failure;
          
          }
          /* Number of histogram buckets for jitter metrics.  */
          else if (strcmp (long_options[option_index].name, "prometheus-jitter-buckets") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_jitter_buckets_arg), 
                 &(args_info->prometheus_jitter_buckets_orig), &(args_info->prometheus_jitter_buckets_given),
                &(local_args_info.prometheus_jitter_buckets_given), optarg, 0, "100", ARG_INT,
                check_ambiguity, override, 0, 0,
                "prometheus-jitter-buckets", '-',
                additional_error))
              goto failure;
          
          }
          /* Minimum jitter bucket boundary, TIME units.  */
          else if (strcmp (long_options[option_index].name, "prometheus-jitter-min") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_jitter_min_arg), 
                 &(args_info->prometheus_jitter_min_orig), &(args_info->prometheus_jitter_min_given),
                &(local_args_info.prometheus_jitter_min_given), optarg, 0, "100us", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "prometheus-jitter-min", '-',
                additional_error))
              goto failure;
          
          }
          /* Maximum jitter bucket boundary, TIME units.  */
          else if (strcmp (long_options[option_index].name, "prometheus-jitter-max") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_jitter_max_arg), 
                 &(args_info->prometheus_jitter_max_orig), &(args_info->prometheus_jitter_max_given),
                &(local_args_info.prometheus_jitter_max_given), optarg, 0, "200ms", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "prometheus-jitter-max", '-',
                additional_error))
              goto failure;
          
          }
          /* Bucket spacing for jitter histogram.  */
          else if (strcmp (long_options[option_index].name, "prometheus-jitter-scale") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_jitter_scale_arg), 
                 &(args_info->prometheus_jitter_scale_orig), &(args_info->prometheus_jitter_scale_given),
                &(local_args_info.prometheus_jitter_scale_given), optarg, cmdline_parser_prometheus_jitter_scale_values, "log", ARG_ENUM,
                check_ambiguity, override, 0, 0,
                "prometheus-jitter-scale", '-',
                additional_error))
              goto failure;
          
          }
          /* Number of histogram buckets for RTT metrics.  */
          else if (strcmp (long_options[option_index].name, "prometheus-rtt-buckets") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_rtt_buckets_arg), 
                 &(args_info->prometheus_rtt_buckets_orig), &(args_info->prometheus_rtt_buckets_given),
                &(local_args_info.prometheus_rtt_buckets_given), optarg, 0, "100", ARG_INT,
                check_ambiguity, override, 0, 0,
                "prometheus-rtt-buckets", '-',
                additional_error))
              goto failure;
          
          }
          /* Minimum RTT bucket boundary, TIME units.  */
          else if (strcmp (long_options[option_index].name, "prometheus-rtt-min") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_rtt_min_arg), 
                 &(args_info->prometheus_rtt_min_orig), &(args_info->prometheus_rtt_min_given),
                &(local_args_info.prometheus_rtt_min_given), optarg, 0, "1ms", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "prometheus-rtt-min", '-',
                additional_error))
              goto failure;
          
          }
          /* Maximum RTT bucket boundary, TIME units.  */
          else if (strcmp (long_options[option_index].name, "prometheus-rtt-max") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_rtt_max_arg), 
                 &(args_info->prometheus_rtt_max_orig), &(args_info->prometheus_rtt_max_given),
                &(local_args_info.prometheus_rtt_max_given), optarg, 0, "100ms", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "prometheus-rtt-max", '-',
                additional_error))
              goto failure;
          
          }
          /* Bucket spacing for RTT histogram.  */
          else if (strcmp (long_options[option_index].name, "prometheus-rtt-scale") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->prometheus_rtt_scale_arg), 
                 &(args_info->prometheus_rtt_scale_orig), &(args_info->prometheus_rtt_scale_given),
                &(local_args_info.prometheus_rtt_scale_given), optarg, cmdline_parser_prometheus_rtt_scale_values, "log", ARG_ENUM,
                check_ambiguity, override, 0, 0,
                "prometheus-rtt-scale", '-',
                additional_error))
              goto failure;
          
          }
          /* Maximum network packet size, SIZE units.  */
          else if (strcmp (long_options[option_index].name, "max-packet-size") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->max_packet_size_arg), 
                 &(args_info->max_packet_size_orig), &(args_info->max_packet_size_given),
                &(local_args_info.max_packet_size_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "max-packet-size", '-',
                additional_error))
              goto failure;
          
          }
          /* Maximum I/O and processing frame size, SIZE units.  */
          else if (strcmp (long_options[option_index].name, "max-frame-size") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->max_frame_size_arg), 
                 &(args_info->max_frame_size_orig), &(args_info->max_frame_size_given),
                &(local_args_info.max_frame_size_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "max-frame-size", '-',
                additional_error))
              goto failure;
          
          }
          /* Enable self-profiling.  */
          else if (strcmp (long_options[option_index].name, "prof") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->prof_flag), 0, &(args_info->prof_given),
                &(local_args_info.prof_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "prof", '-',
                additional_error))
              goto failure;
          
          }
          /* Dump run-time metrics to specified CSV file.  */
          else if (strcmp (long_options[option_index].name, "dump") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->dump_arg), 
                 &(args_info->dump_orig), &(args_info->dump_given),
                &(local_args_info.dump_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "dump", '-',
                additional_error))
              goto failure;
          
          }
          
          break;
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", CMDLINE_PARSER_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */


  update_multiple_arg((void *)&(args_info->source_arg),
    &(args_info->source_orig), args_info->source_given,
    local_args_info.source_given, 0,
    ARG_STRING, source_list);
  update_multiple_arg((void *)&(args_info->repair_arg),
    &(args_info->repair_orig), args_info->repair_given,
    local_args_info.repair_given, 0,
    ARG_STRING, repair_list);
  update_multiple_arg((void *)&(args_info->control_arg),
    &(args_info->control_orig), args_info->control_given,
    local_args_info.control_given, 0,
    ARG_STRING, control_list);
  update_multiple_arg((void *)&(args_info->miface_arg),
    &(args_info->miface_orig), args_info->miface_given,
    local_args_info.miface_given, 0,
    ARG_STRING, miface_list);

  args_info->verbose_given += local_args_info.verbose_given;
  local_args_info.verbose_given = 0;
  args_info->source_given += local_args_info.source_given;
  local_args_info.source_given = 0;
  args_info->repair_given += local_args_info.repair_given;
  local_args_info.repair_given = 0;
  args_info->control_given += local_args_info.control_given;
  local_args_info.control_given = 0;
  args_info->miface_given += local_args_info.miface_given;
  local_args_info.miface_given = 0;
  
  if (check_required)
    {
      error_occurred += cmdline_parser_required2 (args_info, argv[0], additional_error);
    }

  cmdline_parser_release (&local_args_info);

  if ( error_occurred )
    return (EXIT_FAILURE);

  return 0;

failure:
  free_list (source_list, 1 );
  free_list (repair_list, 1 );
  free_list (control_list, 1 );
  free_list (miface_list, 1 );
  
  cmdline_parser_release (&local_args_info);
  return (EXIT_FAILURE);
}
/* vim: set ft=c noet ts=8 sts=8 sw=8 tw=80 nojs spell : */
